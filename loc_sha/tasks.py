from __future__ import absolute_import, unicode_literals
from celery import shared_task

# @shared_task
# def t_exit(data, group, cid):
# 	data[group]['member'][cid] = None
#     data[group]['num'] -= 1
#     if data[group]['num'] == 0:
#         del data[group]

